/* groovylint-disable-next-line CompileStatic */
node('master') {
    wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName' : 'xterm']) {
        timestamps {
            stage('Checkout Code') {
                sh 'env'
                checkout scm
            }
        }
    }
}

